version: 2

sources:
  - name: staging
    schema: staging
    description: "Raw data from Airbyte syncs"
    tables:
      - name: forms
        description: "Form definitions with nested structure_override JSON containing all field configurations"

      - name: form_records
        description: "Form submissions with dynamic data JSON, fulcrum sync info, and project references"

      - name: projects
        description: "Project definitions with config, phases, finance, timeline, and other nested JSON structures"
